{
  "project": "solidts",
  "version": "0.0.0",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy:netlify": "vite build && netlify deploy --prod",
    "deploy:surge": "vite build && surge ./dist justadirck-solidts.surge.sh"
  },
  "dependencies": {
    "solid-js": "^1.7.3"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "vite": "^4.2.1",
    "vite-plugin-solid": "^2.7.0"
  }
}
